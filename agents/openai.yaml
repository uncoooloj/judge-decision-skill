interface:
  display_name: "Judge Decision"
  short_description: "Debate paths into decisions"
  default_prompt: "Use $judge-decision to challenge this path, ask Claude for an external judgment, and produce a final judge-of-judge decision."
