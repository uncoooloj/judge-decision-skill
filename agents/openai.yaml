interface:
  display_name: "Judge Decision"
  short_description: "Debate paths into decisions"
  default_prompt: "Use $judge-decision to challenge this path, choose the right external judge, and produce a final judge-of-judge decision."
